import React from "react";
import ReactDOM from "react-dom/client";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <p style={{ fontFamily: "Splash" }}>Some text for testing</p>
  </React.StrictMode>
);
